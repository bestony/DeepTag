/**
 * DeepTag entry point.
 *
 * Placeholder module: the TypeScript project needs at least one input file for
 * `tsc` to run. Replace with the real application bootstrap.
 */

export const APP_NAME = "deeptag";
